import { ArgTypes, Markdown, Meta } from "@storybook/blocks";
import { cdnJs, cdnCss } from "../../../globals/internal/storybook-cdn";
import * as ExtendedButtonStories from "./extended-button.stories";

<Meta of={ExtendedButtonStories} />

# Extended Button

[Insert blurb of component and its usage here]

## Getting started

Here's a quick example to get you started.

### JS (via import)

```javascript
import "@carbon/web-components/es/components/extended-button/index.js";
```

<Markdown>{`${cdnJs({ components: ["extended-button"] })}`}</Markdown>
<Markdown>{`${cdnCss()}`}</Markdown>

### HTML

```html
<prefix-extended-button> Extended button </prefix-extended-button>
```

## `<prefix-extended-button>` attributes, properties and events

Note: For `boolean` attributes, `true` means simply setting the attribute (e.g.
`<prefix-extended-button open>`) and `false` means not setting the attribute (e.g.
`<prefix-extended-button>` without `open` attribute).

<ArgTypes of="prefix-extended-button" />
